# Files executed on Linux must keep LF endings even when the repo is
# checked out (or `git archive`d) on Windows with core.autocrlf=true.
*.sh      text eol=lf
*.service text eol=lf
*.timer   text eol=lf
*.py      text eol=lf
requirements.txt text eol=lf
