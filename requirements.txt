# Flight Scraper — Python backend (pool scheduler) runtime deps.
# Pinned to the versions verified working on the dev machine.
# `flights` is the PyPI dist that imports as `fli` (Google Flights wrapper);
# it pulls curl_cffi / pydantic / tenacity / ratelimit / httpx transitively.
flights==0.8.2
pymongo==4.16.0
dnspython==2.8.0          # required by pymongo for mongodb+srv:// (Atlas) URIs
python-dotenv==1.2.1
beautifulsoup4==4.12.3    # azair parser (loaded transitively by scraper-fli)
lxml==5.3.0               # BeautifulSoup backend
requests==2.32.3          # azair scraper
bcrypt==4.2.1             # legacy user_repo (imported at database package level)
apscheduler==3.11.2       # legacy scheduler.py (imported at scheduler package level)
